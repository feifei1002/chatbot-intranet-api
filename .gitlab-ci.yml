image: python:3.11-slim

variables:
  TOGETHER_API_KEY: $TOGETHER_API_KEY

cache:
  paths:
    - /root/.cache/pip

stages:
  - lint
  - test

before_script:
  - pip install -r requirements.txt

lint:
  stage: lint
  script:
    - ruff check .

test:
  stage: test
  script:
    - pytest
